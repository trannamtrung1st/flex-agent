# Transition-only ADR-020 candidate overlay. Not Production and not the
# canonical OIDC acceptance authority.
# Use: bash build/scripts/authenticated-browser-profile.sh --overlay candidate up
services:
  api:
    environment:
      HumanAuthentication__RedirectUri: http://127.0.0.1:5274/auth/callback
