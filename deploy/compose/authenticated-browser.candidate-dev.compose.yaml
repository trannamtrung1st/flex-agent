# Optional override for candidate SPA dev on http://127.0.0.1:5274/.
# Use: COMPOSE_FILE=deploy/compose/authenticated-browser.compose.yaml:deploy/compose/authenticated-browser.candidate-dev.compose.yaml bash build/scripts/authenticated-browser-profile.sh up
services:
  api:
    environment:
      HumanAuthentication__RedirectUri: http://127.0.0.1:5274/auth/callback
