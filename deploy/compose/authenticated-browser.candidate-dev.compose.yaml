# Transition-only ADR-020 candidate overlay. Not Production and not the
# canonical OIDC acceptance authority.
# Use: pnpm compose:candidate (or bash build/scripts/authenticated-browser-profile.sh --overlay candidate up)
services:
  api:
    environment:
      HumanAuthentication__RedirectUri: http://localhost:5274/auth/callback
