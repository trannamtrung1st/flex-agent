import { LiveSessionLayout, type LiveSessionLayoutProps } from "../../design-system";

export function TextSessionStation(props: LiveSessionLayoutProps) {
  return <LiveSessionLayout {...props} />;
}
