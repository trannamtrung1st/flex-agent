<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>FlexAgent.CanonicalJson</RootNamespace>
    <AssemblyName>FlexAgent.CanonicalJson</AssemblyName>
    <Nullable>disable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="FlexAgent.CanonicalJson.Tests" />
  </ItemGroup>

  <ItemGroup>
    <Compile Remove="Upstream/**/*.cs" />
    <Compile Include="Upstream/CyberphoneJsonCanonicalization/**/*.cs">
      <Nullable>disable</Nullable>
    </Compile>
  </ItemGroup>

</Project>
